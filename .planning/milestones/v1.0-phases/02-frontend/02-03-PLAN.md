---
phase: 02-frontend
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - REND-01
  - REND-02
  - REND-03
  - REND-04
user_setup: []

must_haves:
  truths:
    - "Running `python main.py` against a real feeds.yaml produces an output HTML file with the new structure"
    - "User visually confirms each RSS row shows source-tinted core_idea quote box below the title (REND-01) and a numbered key-points list with source-colored chips inside the dropdown (REND-02)"
    - "User visually confirms 'Read full article ↗' link still appears at the bottom of every expanded dropdown (REND-03)"
    - "User visually confirms a fallback-case article renders as a plain link with no broken layout (REND-04)"
    - "User visually confirms GitHub and Reddit accordion sections look identical to before this phase"
    - "User visually confirms highlight cards show core_idea as a plain serif paragraph above the bordered reason quote (D-09, D-10)"
  artifacts:
    - path: "output/{date}.html"
      provides: "Human-verifiable rendered digest with all Phase 2 visual changes live"
      contains: "article-core-idea, article-keypoint-chip, hl-core-idea, acc-index"
  key_links:
    - from: "main.py orchestrator"
      to: "_render_rss_items() and _render_highlights()"
      via: "render_html_digest invocation"
      pattern: "render_html_digest"
---

<objective>
End-to-end visual verification: run `python main.py` against the real `feeds.yaml`, open the resulting HTML in Chrome, and have the user confirm the four success criteria from ROADMAP §Phase 2 hold. This is the only gate that catches visual issues no grep can detect (e.g., the core_idea quote box rendering with the wrong color, key-points list spilling outside the dropdown body, or accordion sections subtly shifting).

Purpose: Tasks in Plans 01 and 02 verify the structural and grep-checkable invariants. This plan verifies the lived visual experience that the user actually consumes each morning.

Output: A user "approved" signal (or a list of issues to feed back into a follow-up plan via `/gsd-plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-frontend/02-CONTEXT.md
@.planning/phases/02-frontend/02-01-PLAN.md
@.planning/phases/02-frontend/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run main.py and produce today's digest</name>
  <files></files>
  <read_first>
    - main.py (full file — confirm `python main.py` is the canonical run command per CLAUDE.md and that output writes to `output/{date}.html`)
    - feeds.yaml (confirm it has at least one RSS feed configured, since RSS items are the only ones with core_idea/key_points)
  </read_first>
  <behavior>
    - `python main.py` exits 0.
    - A new HTML file is written to `output/{today}.html` (where `{today}` = YYYY-MM-DD).
    - The file contains the new classes (`article-core-idea`, `article-keypoint-chip`, `hl-core-idea`).
    - The file contains the unchanged accordion classes (`acc-index`, `acc-summary-box`).
    - macOS auto-open via `open -a "Google Chrome"` triggers (per CLAUDE.md "Auto-open" — the existing `subprocess.Popen` in `main.py:100`).
  </behavior>
  <action>
Run the project per CLAUDE.md instructions:

```bash
# Activate venv (per CLAUDE.md Setup section)
source .venv/bin/activate

# Confirm ANTHROPIC_API_KEY is exported (required by Phase 1 summarizer)
test -n "${ANTHROPIC_API_KEY}" || { echo "ERROR: ANTHROPIC_API_KEY not exported"; exit 1; }

# Run the digest (writes to output/YYYY-MM-DD.html and auto-opens in Chrome)
python main.py
```

After completion, capture:
- The output filename (`output/YYYY-MM-DD.html`).
- A grep summary confirming the four new classes are present:
  ```bash
  grep -c -F 'class="article-core-idea"'   output/$(date +%Y-%m-%d).html
  grep -c -F 'class="article-keypoints"'   output/$(date +%Y-%m-%d).html
  grep -c -F 'class="article-keypoint-chip"' output/$(date +%Y-%m-%d).html
  grep -c -F 'class="hl-core-idea"'        output/$(date +%Y-%m-%d).html
  ```
- A grep summary confirming accordion classes are still present:
  ```bash
  grep -c -F 'class="acc-index"'           output/$(date +%Y-%m-%d).html
  grep -c -F 'class="acc-summary-box"'     output/$(date +%Y-%m-%d).html
  ```

Karpathy/simplicity-first per CLAUDE.md:
- Do NOT touch any source code — this is a run/observe task only.
- If the run fails for an unrelated Phase 1 reason (e.g., a feed times out), note it in the SUMMARY but still attempt to proceed if `output/{date}.html` was produced for the surviving sources.
  </action>
  <verify>
    <automated>test -f "output/$(date +%Y-%m-%d).html" && grep -q -F 'class="article-core-idea"' "output/$(date +%Y-%m-%d).html" && grep -q -F 'class="article-keypoint-chip"' "output/$(date +%Y-%m-%d).html" && grep -q -F 'class="hl-core-idea"' "output/$(date +%Y-%m-%d).html" && grep -q -F 'class="acc-index"' "output/$(date +%Y-%m-%d).html" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `output/$(date +%Y-%m-%d).html` exists and is non-empty.
    - All four `class="article-core-idea"`, `class="article-keypoints"`, `class="article-keypoint-chip"`, `class="hl-core-idea"` grep counts are >= 1.
    - Both `class="acc-index"`, `class="acc-summary-box"` grep counts are >= 1 (accordion sections rendered, success criterion 4 prerequisite).
    - `python main.py` exit code is 0.
  </acceptance_criteria>
  <done>The real digest for today exists on disk, contains the new structure, and is open in Chrome ready for visual inspection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies the four ROADMAP success criteria visually</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md §Phase 2 Frontend success criteria (1–4)
    - .planning/phases/02-frontend/02-CONTEXT.md (D-01..D-12)
    - output/{today}.html (the rendered digest from Task 1 — already open in Chrome)
  </read_first>
  <what-built>
    Plans 01 and 02 added new CSS classes (`.article-core-idea`, `.article-keypoints`, `.article-keypoint-chip`, `.hl-core-idea`) and extended `_render_rss_items()` and `_render_highlights()` so each RSS article shows:
    - A source-tinted italic quote box (the core idea) directly below the article title, always visible without expanding.
    - A numbered key-points list (1..N, renumbered to skip empty slots) inside the article dropdown.
    - The "Read full article ↗" link still at the bottom of every dropdown body.
    - For articles with no `core_idea` and no `key_points`, the row falls back to the prior plain-link rendering.
    Highlight cards now show the core idea as a plain serif paragraph above the existing bordered reason quote.
    GitHub and Reddit accordion sections were not modified and should look identical to prior runs.
  </what-built>
  <how-to-verify>
    The digest is open in Chrome (auto-opened by `main.py`). Walk through these checks in order. After each, note pass/fail.

    **REND-01 / D-02 — core idea always visible (newspaper-deck pattern):**
    1. Scroll to the "All Stories" RSS section.
    2. Pick any RSS article that has an AI breakdown (most should).
    3. Confirm: directly below the article title row you see a left-bordered, italic, slightly-greyed quote box containing one short sentence (the core idea). The left border color matches the source chip color above it.
    4. Confirm: this box is visible WITHOUT clicking the chevron — no need to expand the dropdown to read it.

    **REND-02 / D-05 / D-06 / D-07 — numbered key points inside dropdown:**
    1. Click the chevron (▼) on the article you just inspected.
    2. Confirm: a numbered list of key points appears inside the dropdown body (typically 5 items, fewer if Phase 1 returned partial fills).
    3. Confirm: each row has a small circular numbered chip (1, 2, 3, …) on the left, colored with the same source color as the source chip and the core-idea border.
    4. Confirm: the rows are compact — small font, tight vertical spacing — and the last row has no bottom border.
    5. Pick a different RSS article and confirm chip numbering re-starts at 1 for that article.

    **REND-03 / D-08 — Read full article link:**
    1. With the dropdown still expanded, scroll to the bottom of the dropdown body.
    2. Confirm: the small "Read full article ↗" link is still there, source-colored, and clicking it opens the article in a new tab.

    **REND-04 / D-04 — fallback case:**
    1. Look for any RSS article that does NOT have a chevron / dropdown (this happens when Phase 1's fetch failed for that URL and the fallback yielded no `core_idea` and no `key_points`).
    2. If such a row exists in today's digest: confirm it renders as a plain underlined-on-hover link with no broken layout, no orphaned chevron, no empty quote box.
    3. If NO such row exists today (every article got AI breakdown), open the file with a text editor and confirm by inspection that the rendering code path supports it (or trust Plan 02's smoke test which exercised this exact case).

    **Success criterion 4 / D-12 — GitHub & Reddit accordion unchanged:**
    1. Scroll down to the "More Sources" section.
    2. Open the GitHub accordion (click the header).
    3. Confirm: the section looks identical to how it looked before this phase — same green/teal gradient header, same circular numbered chips next to each repo, same "Read full article" link inside any expanded item dropdown, NO core-idea quote box, NO key-points list.
    4. Repeat for the Reddit accordion (orange/yellow header).

    **Highlight cards / D-09 / D-10 / D-11:**
    1. Scroll back up to "Top 5 Highlights of the Day".
    2. For each highlight card with a non-empty core_idea, confirm the body reads in this order: source badge → article title → core idea (plain serif paragraph, no border) → reason (italic with left border).
    3. Confirm the core-idea paragraph is visually distinct from the reason quote (no border vs. bordered).
    4. If any highlight card has an empty core_idea, confirm only the source badge → title → reason are shown — no orphan empty paragraph.
  </how-to-verify>
  <action>
This is a human-verify checkpoint. The executor (Claude) does NOT take any action that modifies code or files. Claude's role:

1. Confirm the file from Task 1 exists at `output/$(date +%Y-%m-%d).html` and is open in Chrome (auto-opened by `main.py`). If it is not open, run `open -a "Google Chrome" "output/$(date +%Y-%m-%d).html"`.
2. Print the `how-to-verify` checklist above to the user verbatim, formatted as a numbered list, and pause for the user's `resume-signal`.
3. Do NOT advance until the user replies with `approved`, `partial: <list>`, or `blocked: <reason>`.
4. Do NOT attempt to inspect the rendered output yourself — color, layout, and visual hierarchy are not grep-checkable; this checkpoint exists precisely because automation cannot judge them.
  </action>
  <verify>
    <automated>test -f "output/$(date +%Y-%m-%d).html" && echo "FILE_READY_FOR_HUMAN_VERIFY" || { echo "MISSING — Task 1 must produce the digest first" >&2; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - The output HTML file from Task 1 exists at `output/$(date +%Y-%m-%d).html`.
    - The user has been presented with the full `how-to-verify` checklist above.
    - The user has replied with one of `approved`, `partial: <list>`, or `blocked: <reason>` (per resume-signal).
    - On `approved`: phase verification is complete; record the signal in `02-03-SUMMARY.md`.
    - On `partial:`/`blocked:`: the orchestrator routes to `/gsd-plan-phase --gaps` with the user's list; do not silently mark complete.
  </acceptance_criteria>
  <done>User has issued one of the three resume-signal values verbatim. The signal (and any failure list) is recorded in `02-03-SUMMARY.md`.</done>
  <resume-signal>
    Reply with one of:
    - `approved` — all checks pass, Phase 2 is complete.
    - `partial: <list>` — one or more checks failed; describe which (e.g., "REND-02: chips are not source-colored, they appear grey"). The orchestrator will hand the list to `/gsd-plan-phase --gaps` to schedule fixes.
    - `blocked: <reason>` — could not verify (e.g., "main.py crashed before producing the file" or "no RSS items have AI breakdown today, can't visually confirm REND-01"). Provide the reason and any logs.
  </resume-signal>
</task>

</tasks>

<verification>
- The Wave 1 plans (01 and 02) verified the structural correctness via grep + behavior tests.
- This plan verifies the visual + lived experience that grep cannot see:
  - Source-color consistency between source chip / core-idea border / chip backgrounds.
  - Layout stability when `core_idea` is present (the deck box doesn't push other elements off-grid).
  - Compact density of the key-points list (D-06 — easy to see if a row is too tall).
  - Highlight card hierarchy (D-09 / D-10 — easy to see if `.hl-core-idea` is wrongly italic or bordered).
  - Accordion sections subtly unchanged (success criterion 4 — only the eye can confirm "looks identical to before").
</verification>

<success_criteria>
- User reply is `approved`, OR
- User reply is `partial:`/`blocked:` with a structured list of issues that can be fed into `/gsd-plan-phase --gaps`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend/02-03-SUMMARY.md` recording:
- The output filename inspected.
- Grep counts for the new + accordion classes in the rendered file.
- The user's resume-signal verbatim.
- Any follow-up gaps to schedule.
</output>
